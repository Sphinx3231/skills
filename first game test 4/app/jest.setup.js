// jest-expo's preset already wires up the React Native / Expo native module
// mocks. Anything screen- or hook-specific (expo-router, @clerk/expo, etc.)
// is mocked per test file instead, since most tests need to control what
// those return.
