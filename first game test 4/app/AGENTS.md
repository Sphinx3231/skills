# Expo version

Installed SDK is **54** (verified via `require('expo/package.json').version`,
not just `package.json`'s `^54.0.0` range — check that directly if this ever
seems stale again rather than trusting either file). Use
https://docs.expo.dev/versions/v54.0.0/ for API references. A prior version
of this note pointed at v57 docs, which didn't match what's actually
installed — corrected 2026-08-08 while investigating for
docs/plans/voice-barcode-plan.md.
